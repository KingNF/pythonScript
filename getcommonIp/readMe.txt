需求：
	某文件夹下有多个txt，每个txt的每一行为一个ip。统计下这些所有不同的txt中有没有相同的ip或者ip在同一个c段中（比如192.168.1.2和192.168.1.3就在同一个c段中），将相同的ip或同c段的ip找出来并标出其所在哪个txt中。将找的结果写入一个新的txt中。

使用方法：
	1、将脚本中dir_path 路径改为txt文件夹路径
	2、修改脚本中resultPath 路径为结果文件路径，默认为当前文件夹下common_ip.txt

注意事项：
	1、支持文件夹下存在子文件情况